# Конфигурация мобильного приложения

## Настройка URL для разных сценариев

Приложение автоматически выбирает правильный URL на основе режима работы (`__DEV__`).

### 1. Режим разработки (Development Mode)

**Когда использовать:** Устройство подключено к той же локальной сети (192.168.88.x)

**Настройки:**
- API URL: `http://192.168.88.24/api`
- Traccar GPS: `http://192.168.88.24`
- Traccar API: `http://192.168.88.24/traccar/api`

**Как запустить:**
```bash
npx expo start
```

Приложение автоматически использует локальные URL в режиме разработки.

### 2. Продакшн режим (Production Mode)

**Когда использовать:** Доступ через интернет, устройство НЕ в локальной сети

**Настройки:**
- API URL: `http://85.113.27.42:8530/api`
- Traccar GPS: `http://85.113.27.42:8530`
- Traccar API: `http://85.113.27.42:8530/traccar/api`

**Как запустить:**
```bash
npx expo start --no-dev --minify
```

## Требования для работы

### Backend (Spring Boot)

1. **application.properties** должен содержать:
   ```properties
   server.port=8083
   server.address=0.0.0.0
   ```

2. Запуск:
   ```bash
   ./gradlew bootRun
   ```

### Nginx

1. Должен слушать на `0.0.0.0:80`
2. Должен проксировать запросы к Spring Boot (192.168.88.24:8083)

### MikroTik Router

1. **Внешний порт:** 8530 (с geo-фильтрацией для КР)
2. **NAT правило:** 8530 → 192.168.88.24:80
3. **Geo-фильтр:** Только IP адреса Кыргызстана

### Firewall (Windows)

Открыть порты:
```powershell
New-NetFirewallRule -DisplayName "Nginx Port 80" -Direction Inbound -Protocol TCP -LocalPort 80 -Action Allow
New-NetFirewallRule -DisplayName "Spring Boot 8083" -Direction Inbound -Protocol TCP -LocalPort 8083 -Action Allow
```

## Тестовые учетные данные

### Администратор
- **ИНН:** `admin_user`
- **Пароль:** `admin123`

### Клиент (осужденный)
- **ИНН:** `1234567890123` ⚠️ **13 цифр!**
- **Пароль:** `password123`

## Диагностика проблем

### Проверка доступности backend

**Из локальной сети:**
```bash
curl http://192.168.88.24/api/traccar/devices
```

**Из интернета:**
```bash
curl http://85.113.27.42:8530/api/traccar/devices
```

### Проверка портов

**Spring Boot:**
```powershell
netstat -ano | findstr :8083
```

**Nginx:**
```powershell
netstat -ano | findstr :80
```

### Логи приложения

При запуске Expo следите за логами в консоли:
```
🔐 LOGIN ATTEMPT: { email: '...', password: '***' }
📡 API BASE_URL: http://...
```

При ошибке входа:
```
❌ LOGIN ERROR: ...
Error response: ...
Error status: ...
```

## Частые проблемы

### 1. "Сервер недоступен"
- ✅ Проверьте, что Spring Boot запущен
- ✅ Проверьте, что Nginx запущен
- ✅ Проверьте firewall
- ✅ Проверьте, что устройство в нужной сети (для dev mode)

### 2. "Ошибка входа" / "Неверные учетные данные"
- ✅ ИНН клиента должен быть **13 цифр**: `1234567890123`
- ✅ Пароль: `password123`
- ✅ Проверьте, что в базе данных есть этот клиент (V10 миграция)

### 3. Запросы не доходят до backend
- ✅ Проверьте логи Spring Boot - должны появляться POST запросы к `/api/auth/login`
- ✅ Проверьте URL в логах Expo - должен совпадать с ожидаемым
- ✅ Проверьте, что используется правильный режим (dev/prod)

### 4. Ошибка сети при доступе извне
- ✅ Проверьте, что MikroTik NAT настроен правильно (8530 → 80)
- ✅ Проверьте, что geo-фильтр не блокирует ваш IP
- ✅ Проверьте, что используется продакшн режим с портом 8530

## Изменение конфигурации вручную

Если нужно изменить URL вручную, отредактируйте файл:
```
iosandroid/src/utils/constants.js
```

```javascript
export const ENV_URLS = {
  DEVELOPMENT: 'http://192.168.88.24/api',  // Локальная сеть
  PRODUCTION: 'http://85.113.27.42:8530/api',  // Внешний доступ
};
```

После изменения перезапустите Expo:
```bash
npx expo start --clear
```
